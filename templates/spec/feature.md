# <機能名>

> 目次: [仕様インデックス](index.md) ／ 技術仕様: [README](../README.md)
>
> このファイルは1つの機能の仕様です。機能ごとに1ファイル作ります。

---

## 概要

<何をする機能か、1〜2行で>

---

## 動作

操作・処理の流れを手順で書く：

1. <手順1>
2. <手順2>
3. <手順3>

---

## 受け入れ基準（AC）

> **AC（受け入れ基準）**：「Acceptance Criteria」の略で、「これができたら完成」と言える条件のこと。
> テストを書くときの目印にもなります。

- AC-1: <条件1>
- AC-2: <条件2>
- AC-3: <条件3>

---

## データモデル（必要な場合のみ）

```
<エンティティ>
- id: string
- ...
```

---

## この機能で扱う大事なデータ

> 当てはまるものに ✅ を付けるだけで大丈夫です。1つでも当てはまると、エージェントが安全のための要件（保存場所・見られる範囲・入力チェックなど）を自動でこの仕様に足してくれます。

- [ ] 個人情報（名前・メールアドレス・住所など）
- [ ] パスワード・ログイン情報
- [ ] API キーなどの秘密情報
- [ ] お金・支払いに関する情報
- [ ] 社内限定の情報（他社に見せられないもの）
- [ ] どれも扱わない

---

## 非機能要件（必要な場合のみ）

- **性能**：<例：応答の最初の文字が3秒以内に表示される>
- **セキュリティ**：<例：API キーはサーバー側のみで保持する（「扱う大事なデータ」に ✅ がある場合、エージェントがここを具体化します）>

---

## 未確定事項

- <この機能でまだ決まっていないこと>

---
---

# 【記入サンプル】システムプロンプト設定

> ここから下は書き方のサンプルです。実際に書くときは上のテンプレに記入し、ここは削除してください。

## 概要

システムプロンプト（AI への指示文）を UI から変更・保存できる。

## 動作

1. 設定アイコンをクリックすると設定パネルが開く
2. テキストエリアに任意のシステムプロンプトを入力
3. 保存ボタンで永続化（localStorage）
4. 以降のチャットに反映される

## 受け入れ基準（AC）

- AC-1: 保存したシステムプロンプトはリロード後も残る
- AC-2: 既定値に戻すボタンで初期状態に戻せる
- AC-3: 空文字保存はバリデーションで阻止される

## データモデル

```
Settings
- systemPrompt: string
```

Settings は localStorage に永続化する。

## この機能で扱う大事なデータ

- [x] どれも扱わない（システムプロンプトは利用者本人の設定値のみ）

## 非機能要件

- **セキュリティ**：保存値はクライアント内のみ。サーバーには送らない。API キー等の秘密情報をシステムプロンプトに書かないよう、保存時に注意書きを表示する。

## 未確定事項

- 複数のシステムプロンプトを切り替えて保存できるようにするか
